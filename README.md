# NovelAI-bot

## Setup
```
npm install
npm run build
npm start
```
Fill up `.env` and `config.json` under `prod` folder

## Usage
```
npm start
```
- **ping** : get websocket latency
- **generate** : generate image
